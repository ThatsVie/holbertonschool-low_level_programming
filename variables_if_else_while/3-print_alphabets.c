#include <stdio.h>
/**
 * main - alpABET
 *
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 'a'; n <= 'z' ; n++)
	putchar(n);

	for (n = 'A'; n <= 'Z' ; n++)
	putchar(n);

putchar('\n');
return (0);
}
